import type { PropsWithChildren } from "react";

const Input = (prop: PropsWithChildren) => {
  return (
    <input className="border rounder h-14 position-absolute bottom-0"/>
  )
}

export default Input;